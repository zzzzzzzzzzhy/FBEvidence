[cryptoMaterial]
certPath = "conf"
useSMCrypto = false

[network]
peers = ["127.0.0.1:20200", "127.0.0.1:20201"]
defaultGroup = "group1"

# [account]
# keyStoreDir = "account"  
# accountFileFormat = "pem"
# 注释掉账户文件配置，使用代码生成的密钥对

[threadPool]
threadPoolSize = 16